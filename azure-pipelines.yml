# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
name: 0.1.$(Rev:r)

variables:
- group: theale-prism-central

trigger:
- master

container: ntnx/calm-dsl:latest

steps:
- script: printenv


# stages:
# - stage: Deploy
#   jobs:
#   - deployment: DeployCalm
#     displayName: 'Deploy Calm Blueprint'
#     pool: 'default'
#     container: ntnx/calm-dsl:latest
#     environment: 'dev'
#     strategy:
#       runOnce:
#         deploy:
#           steps:
#           - script: ls -la

#           - checkout: self

#           # - script: |
#           #       docker run --rm -v $(pwd):/root/blueprint:Z ntnx/calm-dsl /bin/bash -c " \
#           #       calm init dsl -i $(pc_ip) -P $(pc_port) -u $(pc_user) -p $(pc_pass) -pj $(calm_projectName) \
#           #       && calm compile bp -f blueprint/main.py"
            
